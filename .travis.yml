language: node_js

os: linux

matrix:
  include:   
    - node_js: '6'
    - node_js: '8'
    - node_js: '10'

sudo: false

install:
  - npm install

script:
  - npm test

after_success:
  - cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage
